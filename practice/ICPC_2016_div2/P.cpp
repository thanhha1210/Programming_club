#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef pair<long long, long long> pll;
typedef set<long long> sll; 
sll cam;
ll n, k, x, y, dist = 0;

int main() {
    cin >> n >> k >> r;
    rep(i, j, n)
}