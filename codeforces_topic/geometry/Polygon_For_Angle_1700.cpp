#include <bits/stdc++.h>
using namespace std;

// 180 = 2 * 2 * 3 * 3 * 5

int main() {
    int t , n;
    double x;
    cin >> t;

    while (t--) {
        cin >> x;
        for (int i = 3; i < n; )
    }
}